<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/container_data_fields"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:orientation="vertical">

        <EditText
            android:id="@+id/editText_nickname"
            style="@style/editTextStyle"
            android:hint="Nickname" />

        <EditText
            android:id="@+id/editText_password"
            style="@style/editTextStyle"
            android:hint="Desired password"
            android:inputType="textPassword"
            android:minEms="8" />

        <EditText
            android:id="@+id/editText_password_confirm"
            style="@style/editTextStyle"
            android:hint="Password Again"
            android:inputType="textPassword"
            android:minEms="8"
            android:visibility="gone" />

        <CheckBox
            android:id="@+id/checkBox_helper"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="I want to help others when needed"
            android:textColor="@android:color/white" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/container_data_fields"
            android:layout_marginLeft="5dp"
            android:layout_marginTop="10dp"
            android:gravity="center_vertical"
            android:orientation="vertical">

            <Button
                android:id="@+id/button_register"
                style="@style/loginButtonStyle"
                android:text="Register" />

        </LinearLayout>

    </LinearLayout>

</RelativeLayout>